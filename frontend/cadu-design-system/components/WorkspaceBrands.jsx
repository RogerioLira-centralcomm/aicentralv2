import React, {useMemo, useState} from 'react';
import {CaduDock} from './CaduDock';
import {CaduSolutionSwitcher} from './WorkspaceSelectors';
import {VisualIdentity} from './VisualIdentity';
import {WorkspaceAccountMenu} from './WorkspaceFeedback';

export function WorkspaceBrands({bootstrap}) {
  const [query, setQuery] = useState(bootstrap.query || ''); const [creating, setCreating] = useState(false); const [accountOpen, setAccountOpen] = useState(false);
  const needle = query.trim().toLocaleLowerCase('pt-BR');
  const brands = useMemo(() => (bootstrap.brands || []).filter(brand => !needle || `${brand.name} ${brand.sector} ${brand.summary}`.toLocaleLowerCase('pt-BR').includes(needle)), [bootstrap.brands, needle]);
  const solutions = [['workspace','Workspace','Projetos e contexto'],['planner','Planner','Planos e cenários'],['studio','Studio','Criação e análise'],['connect','Reports','Relatórios e resultados'],['skills','Skills','Recursos e automações']].map(([id,name,description]) => ({id,name,description,href:bootstrap.urls.solutions?.[id],icon:bootstrap.solutionIcons?.[id]}));
  return <div className="cadu-ds-home-shell cadu-ds-brands-shell"><CaduDock brands={bootstrap.brands || []} resources={bootstrap.projects || []} shortcutItems={bootstrap.dock?.items || []} usagePercent={bootstrap.usagePercent} userAvatar={bootstrap.user?.avatar} userInitials={bootstrap.user?.name?.slice(0,2).toUpperCase()} onNewConversation={() => window.location.assign(bootstrap.urls.newConversation)} onManageShortcuts={() => window.location.assign(bootstrap.urls.home)} onOpenBrand={item => item.href && window.location.assign(item.href)} onOpenResource={item => item.href && window.location.assign(item.href)} onOpenUsage={() => setAccountOpen(true)}/><main className="cadu-ds-home-main"><header className="cadu-ds-home-navbar"><CaduSolutionSwitcher logo={bootstrap.caduMark} solutions={solutions} activeId="workspace"/><a className="cadu-ds-brands-nav" href={bootstrap.urls.projects}>Projetos</a><strong>Marcas</strong><div className="cadu-ds-project-navbar__spacer"/><button className="cadu-ds-home-account" onClick={() => setAccountOpen(true)}><VisualIdentity src={bootstrap.user?.avatar} initials={bootstrap.user?.name} label={bootstrap.user?.name}/></button></header><section className="cadu-ds-brands-content"><header><div><p>Gestão de marca</p><h1>Identidades que orientam o trabalho</h1><span>Reúna marca, referências e direção antes de levar o contexto para um projeto.</span></div><button className="is-primary" onClick={() => setCreating(true)}>Nova marca</button></header><div className="cadu-ds-brands-tools"><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Buscar marca"/><span>{brands.length} marca{brands.length === 1 ? '' : 's'}</span></div><div className="cadu-ds-brands-grid">{brands.map(brand => <a href={brand.href} key={brand.id}><VisualIdentity src={brand.logoUrl} initials={brand.visualInitials} label={brand.name} color={brand.visualColor}/><div><small>{brand.sector || 'Marca'}</small><b>{brand.name}</b><p>{brand.summary || (brand.audited ? 'Identidade analisada e disponível.' : 'Adicione referências para construir a identidade.')}</p><em>{brand.audited ? 'Analisada' : 'Em construção'} · {brand.assetCount} ativo{brand.assetCount === 1 ? '' : 's'}</em></div></a>)}{!brands.length && <p className="cadu-ds-brands-empty">Nenhuma marca corresponde à busca.</p>}</div></section></main><WorkspaceAccountMenu open={accountOpen} onClose={() => setAccountOpen(false)} user={bootstrap.user} links={bootstrap.urls} onManageShortcuts={() => window.location.assign(bootstrap.urls.home)}/>{creating && <dialog open className="cadu-ds-dialog cadu-ds-project-dialog"><form className="cadu-ds-project-form" method="post" encType="multipart/form-data" action={bootstrap.urls.createBrand}><input type="hidden" name="_csrf" value={bootstrap.csrf}/><header><div><h2>Nova marca</h2><p>Comece com o que já existe; o time revisa a análise antes de usar a identidade.</p></div><button type="button" onClick={() => setCreating(false)}>×</button></header><label>Nome da marca<input name="name" required minLength="2" maxLength="150"/></label><label>Site oficial<input name="website_url" type="url" maxLength="2000"/></label><label>Setor <small>Opcional</small><input name="sector" maxLength="80"/></label><label>Logo e referências<input name="images" type="file" accept="image/*" multiple/></label><label><input name="primary_logo" type="checkbox" value="true" defaultChecked/> Usar a primeira imagem como logo principal</label><label><input name="analyze" type="checkbox" value="true" defaultChecked/> Analisar a marca após criar</label><footer><button type="button" onClick={() => setCreating(false)}>Cancelar</button><button className="is-primary">Criar marca</button></footer></form></dialog>}</div>;
}
